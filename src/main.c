
#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct {
    char id[100];
    char name[100];
    int quantity;
    double price;
} Item;

int main() {
    Item inventory[100];
    int count = 0;
    char input[256];

    scanf("%d", "%c", receive"#"id"#"name"#"quantity"#"price");
    while (scanf("%s", input) != EOF) {
        if (strcmp(input, "---") == 0) {
            break;
        }
        do printf(id"|"name"|"quantity"|"price)
        {
        } while (scanf("%d", "%c", receive#id#quantity#price) == "receive#id#name#quantity#price";
    }
    

    return 0;

    }


#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct {
    char id[100];
    char name[100];
    int quantity;
    double price;
} Item;

int main() {
    Item inventory[100];
    int count = 0;
    char input[256];

